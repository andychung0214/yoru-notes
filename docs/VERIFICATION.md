# 驗證紀錄

日期：2026-09-23。環境：Windows、Node.js 20.19.0、Playwright 驅動本機 Microsoft Edge（無頭模式）。

## M1 資料與核心
先建立 tests/core.test.cjs 並執行，確認 core.js 尚未存在時測試失敗，再建立 core.js/data.js。首輪 6 項通過；補上儲存錯誤回呼測試後，共 7 項。命令：`node --test tests/core.test.cjs`。

## M2 閱讀介面
瀏覽器首次執行通過 18 項後，在人物頁測試遇到 hashchange 非同步等待問題。加入對人物頁可見狀態的等待後，28 項全數通過；沒有以固定延遲掩蓋問題。網站本身不需額外修改。

## M3 審查與回歸
獨立審查發現跳至內容連結改變 hash，以及損壞資料未提示兩項 Important。先加入 skip-link 回歸測試，實際觀察失敗，再修正連結行為及儲存讀取提示。追加後瀏覽器 30 項通過，無 JavaScript pageerror。

同步修正 minor：重建曲目索引保留鍵盤焦點、主題與人物連結觸控尺寸、對話框取消後回到原觸發元素。

瀏覽器涵蓋：搜尋、切歌、收藏與主題重新整理、人物介紹、JSON 檔案匯入、無效輸入、純文字安全、雙語與單語切換、清除內容、Escape 焦點返回、localStorage 拒絕與損壞、file:// 直接開啟。360／390／768／1024／1440 px 均無水平溢出。

產出並人工檢視 `test-results/desktop.png`、`mobile.png`、`mobile-dialog.png`；另產出人物頁 `artist.png` 與機器可讀 `browser-report.json`。這些可重建檔案不提交 Git。

## 決策與限制
- 使用純靜態資料館而非遊戲：需求核心為音樂資訊閱讀；不加入無關遊戲機制。
- 首版八首精選、來源整理，不冒稱完整目錄。
- 沒有歌詞授權，因此提供匯入與切換，未內附完整歌詞或全文翻譯。
- 新建獨立子目錄儲存庫；父目錄不是 Git 儲存庫，避免納入其他專案。
- 未實测 Safari、Firefox、實體觸控、螢幕閱讀器、完整 WCAG 或外部 MV 播放。測試通過僅代表記錄中的範圍。
- 首版後續已推送至使用者提供的 GitHub 儲存庫，並啟用 feature/yoru-notes 分支根目錄的 GitHub Pages。

## 第二批曲目驗證
新增八首，總計十六首。先將總數與動畫分類測試改為新預期，實際觀察 8 != 16 與 3 != 5 失敗，再加入資料。

- 核心測試：8 項通過，涵蓋 16 個唯一 id、11 首小說／5 首動畫，以及八首新增曲目的中文／原作搜尋與必要欄位。
- 瀏覽器：41 項通過；新增八首逐一開啟並比對標題、原作與 MV，歌詞空狀態正常；新增曲目收藏重新整理後保留。
- 手機：可滑動並開啟第 16 首《三原色》，新增長曲名《あの夢をなぞって》無水平溢出；已檢視手機截圖。
- 八個新增 MV 經 YouTube oEmbed 確認曲名、YOASOBI 發布者與 HTTP 200，不宣稱已實測各地區影片播放。
- app.js/core.js 與既有歌曲 id 不變，既有 localStorage 資料格式不變。
